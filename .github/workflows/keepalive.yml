name: keep-streamlit-awake

on:
  schedule:
    - cron: "0 */4 * * *"   # every 4 hours (UTC)
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit app
        run: |
          set -euo pipefail
          curl --max-time 20 -A "keepalive" "https://job-intelligence-engine.streamlit.app/" > /dev/null
